package abstraction;

// If a class extends from more than one class, then all them will be an interface
public interface Principal extends School, StateEducationBody {
	
	 void holdStaffMeetings();
	 void arrangeParentTeacherMeet();
	

}
